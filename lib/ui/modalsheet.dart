import 'package:flutter/material.dart';

void bot_sheet(BuildContext context) {
  showBottomSheet(
      context: context,
      builder: ((context) {
        return Column(
          children: [],
        );
      }));
}
